{
  "name": "cmp-lite",
  "version": "1.0.0",
  "description": "Lightweight CMP with Google Consent Mode V2 support",
  "private": true,
  "scripts": {
    "build": "node esbuild.config.mjs",
    "build:watch": "node esbuild.config.mjs --watch",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "vitest": "^3.1.0",
    "jsdom": "^26.0.0"
  }
}
